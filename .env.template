# Atlas Environment Configuration Template
# Copy this to .env and update with your production values

# Database Configuration
DATABASE_URL=postgresql://atlas:SECURE_PASSWORD@postgres:5432/atlas?schema=public

# JWT Configuration (Use strong, unique secrets in production)
JWT_SECRET=GENERATE_STRONG_SECRET_HERE
JWT_EXPIRES_IN=3600
JWT_REFRESH_SECRET=GENERATE_STRONG_REFRESH_SECRET_HERE
JWT_REFRESH_EXPIRES_IN=36000

# Google OAuth (Update with your production credentials)
GOOGLE_CLIENT_ID=YOUR_PRODUCTION_CLIENT_ID
GOOGLE_CLIENT_SECRET=YOUR_PRODUCTION_CLIENT_SECRET
GOOGLE_REDIRECT_API_URL=https://yourdomain.com/api/auth/google/callback

# Service URLs (These are correct for Docker)
CAMPAIGN_SERVICE_URL=http://campaign-service:3001
RESEARCH_SERVICE_URL=http://research-service:3002
WEBSOCKET_SERVICE_URL=http://websocket-service:3003

# Performance Configuration
MAX_CONCURRENT_JOBS=20

# Service-specific Configuration
FIRECRAWL_API_KEY=YOUR_FIRECRAWL_API_KEY
GEMINI_API_KEY=YOUR_GEMINI_API_KEY
REDIS_URL=redis://valkey:6379

# IMPORTANT SECURITY NOTES:
# 1. Generate strong secrets using: openssl rand -base64 32
# 2. Use environment-specific values for production
# 3. Never commit secrets to version control
# 4. Use proper SSL certificates in production
# 5. Set up proper firewall rules